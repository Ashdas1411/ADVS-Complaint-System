services:
  backend:
    build: .
    container_name: advs_backend

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - hostel_db:/app

    restart: unless-stopped

volumes:
  hostel_db: